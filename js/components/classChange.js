export function classAdd(elem, className = 'visually-hidden') {
  elem.classList.add(className);
}

export function classRemove(elem, className = 'visually-hidden') {
  elem.classList.remove(className);
}
